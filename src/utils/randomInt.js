/**
 *
 * @param {number} max Max size of the random number.  For example, randomInt(3)
 * would return 9, 1, or 2
 *
 * @returns
 */
export function randomInt(max) {
    return Math.floor(Math.random() * max);
  }
